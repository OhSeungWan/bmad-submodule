{
  "name": "bmad-submodule",
  "version": "1.3.1",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{md,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{md,yaml,yml}\""
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
